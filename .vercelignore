# Vercel upload exclusions (Task 9: keep the serverless bundle lean).
# The MCP server (src/mcp + views) NEVER deploys to Vercel serverless — it
# runs on Manufact/Fly (see MCP.md "Deploy"). views/ is still excluded here
# so a stray import cannot bloat the web bundle.
views/
storybook-static/
cypress/
.mcp-use/
